<h1>Welcome, <%= current_user.username %>!</h1>

<h2><u>Your Private Repos</u></h2>
<ol>
  <% @private_repos.each do |repo| %>
    <li><%= repo.name %></li>
  <% end %>
</ol>

<h2><u>Your Public Repos</u></h2>
<ol>
  <% @public_repos.each do |repo| %>
    <li><%= repo.name %></li>
  <% end %>
</ol>